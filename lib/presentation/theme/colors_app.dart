import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.white;
  static const accentColor = Color(0xFF535353);
  static const textColor = Color(0xFF000000);
  static const iconColor = Colors.white;
}
